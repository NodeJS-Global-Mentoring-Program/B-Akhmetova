export type User = {
    id: string,
    login: string,
    password: string,
    age: number
}

export type mockUser = {
    id: string,
    login: string,
    password: string,
    age: number,
    isDeleted: boolean
}
